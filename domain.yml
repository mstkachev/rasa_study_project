intents:
  - greet
  - goodbye

responses:
  utter_greet:
  - text: "Привет))"

  utter_goodbye:
  - text: "Пока)"

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
